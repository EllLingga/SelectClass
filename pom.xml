<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.selectclass</groupId>
    <artifactId>SelectClassPlugin</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <properties>
        <!-- Sejak Paper 26.1 (Minecraft "Tiny Takeover"), server & paper-api
             butuh Java 25 - Java 21 sudah tidak cukup lagi (beda dari
             versi-versi sebelumnya). Class file paper-api sekarang versi 69
             (Java 25), jadi compiler & JDK yang dipakai build HARUS 25 juga,
             kalau tidak akan muncul error "class file has wrong version". -->
        <maven.compiler.source>25</maven.compiler.source>
        <maven.compiler.target>25</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!-- Skema versi Paper berbasis tahun (26.1.2 = MC 1.21.x drop 26.1, build ke-2).
             Version range dipakai supaya Maven otomatis ambil build terbaru. -->
        <paper.version>[26.1.2.build,)</paper.version>
    </properties>

    <repositories>
        <repository>
            <id>papermc</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
        <repository>
            <id>placeholderapi</id>
            <url>https://repo.extendedclip.com/content/repositories/placeholderapi/</url>
        </repository>
    </repositories>

    <dependencies>
        <dependency>
            <groupId>io.papermc.paper</groupId>
            <artifactId>paper-api</artifactId>
            <version>${paper.version}</version>
            <scope>provided</scope>
        </dependency>
        <!-- Opsional (softdepend): dipakai supaya %selectclass_class%/%selectclass_tag%
             bisa dibaca DiscordSRV lewat integrasi PlaceholderAPI-nya.
             Plugin tetap jalan normal walau PlaceholderAPI tidak terpasang di server -
             lihat pengecekan getPlugin("PlaceholderAPI") di SelectClassPlugin#onEnable. -->
        <dependency>
            <groupId>me.clip</groupId>
            <artifactId>placeholderapi</artifactId>
            <version>2.11.6</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>${project.artifactId}</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <version>3.3.1</version>
            </plugin>
        </plugins>
    </build>
</project>
